# Repo-root environment, used by the seed script (supabase/seed/seed.mjs).
# Copy to .env. The service-role key bypasses RLS — never commit it, and never
# point it at a production project.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
